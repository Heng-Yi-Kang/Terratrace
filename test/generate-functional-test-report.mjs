import { writeFile } from 'fs/promises'
import {
  AlignmentType,
  Document,
  HeadingLevel,
  Packer,
  Paragraph,
  Table,
  TableCell,
  TableRow,
  TextRun,
  WidthType,
} from 'docx'

const outputPath = 'test/functional-test.docx'
const reportDate = '2026-06-11'
const evidenceSummary = 'test/evidence/functional/execution-summary.md'

const modules = [
  {
    name: 'Authentication And Session Management',
    objective: 'Verify user-facing registration, login, logout, route protection, and role-aware navigation behavior.',
    environment: 'Local frontend/backend plus Vitest UI and API evidence.',
    resultSummary: 'Signup/login UI validation and API/session behavior passed through existing UI component and backend integration tests. Full interactive browser session flows were not completed because no browser automation runner is installed.',
    testCases: [
      {
        id: 'FT-AUTH-001',
        module: 'Registration',
        scenario: 'Valid registration creates an account and routes by role.',
        steps: 'Submit valid username, email, password, confirm password, and role from the signup UI.',
        expected: 'Account is created, session is established, and user is routed to the correct authenticated area.',
        actual: 'Signup page functional-style test submitted valid data and verified role-aware navigation. Backend integration verified user creation, hashed password, and session cookie.',
        status: 'Pass',
        evidence: 'frontend SignupPage tests passed; backend auth-profile IT-AUTH-001 passed; frontend suite 93/93 and backend suite 39/39 passed.',
      },
      {
        id: 'FT-AUTH-002',
        module: 'Registration',
        scenario: 'Invalid and duplicate registration input is rejected.',
        steps: 'Submit missing fields, mismatched passwords, short password, invalid email, and a duplicate email.',
        expected: 'The UI/API rejects invalid input and duplicate email without creating a second account.',
        actual: 'AuthForm rendered validation for missing/short/mismatched input; backend duplicate email integration returned rejection.',
        status: 'Pass',
        evidence: 'frontend AuthForm tests passed; backend auth-profile IT-AUTH-002 passed.',
      },
      {
        id: 'FT-AUTH-003',
        module: 'Login',
        scenario: 'Valid and invalid login attempts show the correct outcome.',
        steps: 'Submit valid credentials, then submit wrong credentials.',
        expected: 'Valid credentials navigate to dashboard; invalid credentials show an error and stay on login.',
        actual: 'LoginPage tests verified dashboard navigation on success and visible error without navigation on failure. Backend login integration accepted valid credentials and rejected invalid ones.',
        status: 'Pass',
        evidence: 'frontend LoginPage tests passed; backend auth-profile IT-AUTH-003 passed.',
      },
      {
        id: 'FT-AUTH-004',
        module: 'Session Protection',
        scenario: 'Logout and protected/admin route restrictions are enforced.',
        steps: 'Logout, access protected pages without a session, and access admin pages as a non-admin user.',
        expected: 'Session is cleared, protected routes redirect to login, and non-admin users cannot enter admin pages.',
        actual: 'Logout API cleared the session cookie and current-user endpoint rejected missing sessions. Route redirect behavior was not manually completed in an interactive browser.',
        status: 'Blocked',
        evidence: 'backend auth-profile IT-PROFILE-003 and IT-AUTH-004 passed; browser redirect observation blocked by unavailable automation runner.',
      },
    ],
  },
  {
    name: 'Profile Management',
    objective: 'Verify profile display, username update, password change, and account deletion behavior.',
    environment: 'Vitest UI tests, React Query hook tests, and backend API/database integration tests.',
    resultSummary: 'Profile identity, validation, mutation, password, and delete-confirmation behaviors passed through component/hook/API evidence.',
    testCases: [
      {
        id: 'FT-PROFILE-001',
        module: 'Profile Display',
        scenario: 'Profile page displays username, email, and role.',
        steps: 'Open the authenticated profile page.',
        expected: 'Current username, email, and role are visible.',
        actual: 'ProfileTab rendered username, email, and role from current-user data.',
        status: 'Pass',
        evidence: 'frontend ProfileTab and useUser tests passed.',
      },
      {
        id: 'FT-PROFILE-002',
        module: 'Profile Update',
        scenario: 'Username update succeeds while blank/unchanged values are handled.',
        steps: 'Submit blank username, unchanged username, then a changed username.',
        expected: 'Blank value is rejected, unchanged value skips an unnecessary call, and changed value persists with success feedback.',
        actual: 'ProfileTab tests verified all three visible outcomes; backend integration verified PostgreSQL username persistence and refreshed session.',
        status: 'Pass',
        evidence: 'frontend ProfileTab tests passed; backend auth-profile IT-PROFILE-001 passed.',
      },
      {
        id: 'FT-PROFILE-003',
        module: 'Password Change',
        scenario: 'Password change rejects wrong current password and accepts valid current/new password.',
        steps: 'Submit wrong current password, then submit correct current and new password; retry old and new login.',
        expected: 'Wrong current password fails; new password replaces old credential.',
        actual: 'UI displayed password-change API errors; backend integration verified old password failure and new password success after update.',
        status: 'Pass',
        evidence: 'frontend ProfileTab/useUser tests passed; backend auth-profile IT-PROFILE-002 passed.',
      },
      {
        id: 'FT-PROFILE-004',
        module: 'Account Deletion',
        scenario: 'Account deletion requires explicit confirmation and invalidates access.',
        steps: 'Attempt delete before typing DELETE, then confirm deletion.',
        expected: 'Delete stays disabled until confirmation; deleted account is logged out and cannot use the old session.',
        actual: 'ProfileTab kept delete disabled until DELETE was entered; backend integration deleted the account row and rejected the old session.',
        status: 'Pass',
        evidence: 'frontend ProfileTab test passed; backend auth-profile IT-PROFILE-004 passed.',
      },
    ],
  },
  {
    name: 'Eco-Directory And Location Search',
    objective: 'Verify directory cards, search, city/category filters, no-results states, details, and image fallback behavior.',
    environment: 'Local PostgreSQL seed rows, local backend/frontend route check, frontend UI tests, and backend API/database integration tests.',
    resultSummary: 'Directory list/filter/detail API behavior and URL-backed UI states passed. Local /eco-directory rendered HTTP 200 against seeded data.',
    testCases: [
      {
        id: 'FT-DIR-001',
        module: 'Directory Cards',
        scenario: 'Directory loads place cards with names, categories, cities, and eco details.',
        steps: 'Open /eco-directory with seeded locations.',
        expected: 'Place cards display sustainable options from the locations table.',
        actual: 'Local backend returned functional-test location cards and local /eco-directory returned HTTP 200 rendered HTML.',
        status: 'Pass',
        evidence: `${evidenceSummary}; GET /api/locations returned functional-test rows; GET /eco-directory returned 200.`,
      },
      {
        id: 'FT-DIR-002',
        module: 'Search And Filters',
        scenario: 'Search, city, category, and combined filters update URL/results.',
        steps: 'Search by text and apply city/category filters.',
        expected: 'Only matching places are shown and URL search parameters reflect active filters.',
        actual: 'Frontend filter tests verified URL updates and filtered empty state; backend integration verified query/city/category filtering and invalid category rejection.',
        status: 'Pass',
        evidence: 'frontend EcoDirectoryClient/locationFilters/SearchBar tests passed; backend eco-directory IT-DIR-002 passed.',
      },
      {
        id: 'FT-DIR-003',
        module: 'Details Page',
        scenario: 'Selecting a place opens its details and same-category recommendations.',
        steps: 'Open a place detail page and inspect details/recommendations.',
        expected: 'Existing public IDs show full details; missing IDs return a graceful not-found path.',
        actual: 'Backend detail and recommendation integration passed for existing and missing public IDs. Full browser detail navigation was not interactively executed.',
        status: 'Pass',
        evidence: 'backend eco-directory IT-DIR-003 passed; PlaceCard encoded detail route test passed.',
      },
      {
        id: 'FT-DIR-004',
        module: 'Image Fallback',
        scenario: 'Missing image data falls back gracefully.',
        steps: 'Render seeded locations with null image_url/image_thumb.',
        expected: 'Cards remain usable without broken layout when images are missing.',
        actual: 'Seeded functional-test rows had null image fields and /eco-directory still rendered HTTP 200.',
        status: 'Pass',
        evidence: `${evidenceSummary}; seeded null image rows; /eco-directory HTTP 200.`,
      },
    ],
  },
  {
    name: 'Favourites And Saved Places',
    objective: 'Verify guest local favourites, authenticated save/unsave, duplicate handling, saved page, and sync behavior.',
    environment: 'Frontend hook tests plus backend favourites API/database integration evidence.',
    resultSummary: 'Guest local storage behavior, authenticated persistence, duplicate rejection, delete, and sync behavior passed through hook/API evidence.',
    testCases: [
      {
        id: 'FT-FAV-001',
        module: 'Guest Favourites',
        scenario: 'Guest favourites are stored locally during the browser session.',
        steps: 'As a guest, favourite a place and re-read local favourites.',
        expected: 'Local favourite remains visible in session state.',
        actual: 'useFavourites returned localStorage favourites and updated when a local favourite was added.',
        status: 'Pass',
        evidence: 'frontend useFavourites tests passed.',
      },
      {
        id: 'FT-FAV-002',
        module: 'Authenticated Saved Places',
        scenario: 'Authenticated user can save, list, and unsave a place.',
        steps: 'Save a seeded place, open saved places, then unsave it.',
        expected: 'Saved page lists the place; unsaved place disappears.',
        actual: 'Backend integration saved a seeded location, listed mapped saved places, and deleted the favourite row.',
        status: 'Pass',
        evidence: 'backend eco-directory IT-DIR-004 passed.',
      },
      {
        id: 'FT-FAV-003',
        module: 'Duplicate Handling',
        scenario: 'Duplicate save action does not create duplicate visible cards.',
        steps: 'Save the same place twice as an authenticated user.',
        expected: 'Second save is rejected or ignored and only one saved item exists.',
        actual: 'Backend integration returned duplicate-save rejection and retained one favourite row.',
        status: 'Pass',
        evidence: 'backend eco-directory IT-DIR-004 passed.',
      },
      {
        id: 'FT-FAV-004',
        module: 'Login Sync',
        scenario: 'Guest favourites sync or remain handled correctly after login.',
        steps: 'Create local guest favourites, log in, and trigger sync.',
        expected: 'Successful uploads clear local storage; failed uploads remain local.',
        actual: 'useSyncFavourites tests covered no-op when logged out, successful sync clear, and partial failure retention.',
        status: 'Pass',
        evidence: 'frontend useFavourites tests passed.',
      },
    ],
  },
  {
    name: 'Trips And Green Itinerary',
    objective: 'Verify trip create/list/edit/delete, manual itinerary items, saved/directory insertion, invalid dates, and empty states.',
    environment: 'Frontend TripsTab functional-style component tests and trip payload helper tests.',
    resultSummary: 'Trip create/list/edit/delete, itinerary item insertion, linked place display, validation, and empty states passed through automated TripsTab and useTrips evidence.',
    testCases: [
      {
        id: 'FT-TRIP-001',
        module: 'Trip Creation',
        scenario: 'User creates a trip with valid title and dates.',
        steps: 'Open trips dashboard and submit valid title/date values.',
        expected: 'Trip appears in the trip list.',
        actual: 'TripsTab test opened the trip editor, submitted valid destination/date/budget/interests/eco-score values, and verified the create-trip payload. Persisted-trip rendering verified the created trip list/detail presentation.',
        status: 'Pass',
        evidence: 'frontend TripsTab create and persisted itinerary tests passed; frontend useTrips payload helper tests passed.',
      },
      {
        id: 'FT-TRIP-002',
        module: 'Trip Maintenance',
        scenario: 'User edits and deletes an existing trip.',
        steps: 'Edit trip details, save, then delete the trip.',
        expected: 'Updated details are visible; deleted trip disappears.',
        actual: 'TripsTab tests selected an existing trip, edited destination/budget through the UI, verified the update payload, then deleted the same trip and verified the delete mutation id.',
        status: 'Pass',
        evidence: 'frontend TripsTab edit/delete tests passed.',
      },
      {
        id: 'FT-TRIP-003',
        module: 'Itinerary Items',
        scenario: 'User adds manual and saved/directory place itinerary items.',
        steps: 'Add manual item and insert a saved or directory place into itinerary.',
        expected: 'Items display schedule/day-part details and linked place metadata where applicable.',
        actual: 'TripsTab tests added a manual itinerary item, a saved green place, and a directory-search place, then verified schedule fields and linked place metadata in payloads. Persisted rendering verified schedule/day-part details and the encoded View place link.',
        status: 'Pass',
        evidence: 'frontend TripsTab manual item, saved place, directory place, and persisted itinerary tests passed; frontend useTrips recommendation mapping tests passed.',
      },
      {
        id: 'FT-TRIP-004',
        module: 'Validation And Empty States',
        scenario: 'Invalid dates/required fields are rejected and empty trip states render.',
        steps: 'Submit missing or invalid trip fields and inspect empty trip list.',
        expected: 'Validation blocks invalid input and empty state is clear.',
        actual: 'TripsTab tests rendered the empty trip and itinerary states, blocked missing destination, blocked an end date before the start date, and verified no create call occurred.',
        status: 'Pass',
        evidence: 'frontend TripsTab validation and empty-state tests passed.',
      },
    ],
  },
  {
    name: 'Smart Recommendation',
    objective: 'Verify recommendation input, loading, validation, no-candidate handling, and weather/budget rationale.',
    environment: 'Frontend component tests and implemented backend route; external Gemini/network behavior not fully exercised in browser.',
    resultSummary: 'Component-level visible validation, success, error, and save behavior passed. Live external recommendation generation was not completed.',
    testCases: [
      {
        id: 'FT-RECO-001',
        module: 'Recommendation Results',
        scenario: 'Valid destination/date/budget/interests submission displays recommendations.',
        steps: 'Submit a completed smart recommendation form.',
        expected: 'Ranked recommendations, summary, weather context, and rationale appear.',
        actual: 'SmartRecommendationSection rendered successful mocked recommendation data including summary, shortlist, final recommendations, and rationale.',
        status: 'Pass',
        evidence: 'frontend SmartRecommendationSection tests passed.',
      },
      {
        id: 'FT-RECO-002',
        module: 'Validation',
        scenario: 'Missing required fields are rejected.',
        steps: 'Submit recommendation form with blank destination.',
        expected: 'Validation message appears and no API call is made.',
        actual: 'Component test rendered validation error and confirmed fetch was not called.',
        status: 'Pass',
        evidence: 'frontend SmartRecommendationSection validation test passed.',
      },
      {
        id: 'FT-RECO-003',
        module: 'Unavailable Candidates',
        scenario: 'Unsupported destination or API failure shows a fallback/error state.',
        steps: 'Submit a destination with no candidate support or force API failure.',
        expected: 'User receives a clear fallback or error message.',
        actual: 'Component test rendered API error state and removed prior result.',
        status: 'Pass',
        evidence: 'frontend SmartRecommendationSection API-error test passed.',
      },
      {
        id: 'FT-RECO-004',
        module: 'Live External Service',
        scenario: 'Recommendation uses live weather/budget context from backend providers.',
        steps: 'Submit the form against live backend providers.',
        expected: 'Live backend returns explainable recommendations using weather and budget context.',
        actual: 'Live provider path was not executed in browser during this run.',
        status: 'Blocked',
        evidence: 'External Gemini/weather/provider behavior not captured; component and backend implementation present.',
      },
    ],
  },
  {
    name: 'Weather Forecast',
    objective: 'Verify forecast display, values, invalid city handling, date edge cases, and loading/error states.',
    environment: 'Frontend component tests plus implemented Open-Meteo backend route.',
    resultSummary: 'Component-level forecast validation, success, filtering, and error behavior passed. Live provider call was not captured as browser evidence.',
    testCases: [
      {
        id: 'FT-WEATHER-001',
        module: 'Forecast Display',
        scenario: 'Valid city/date search displays forecast cards.',
        steps: 'Search weather for a valid city and selected date range.',
        expected: 'Forecast cards show city, date, temperature, condition, and icon.',
        actual: 'WeatherForecastSection rendered mocked successful forecast response with city heading, searched city, temperature, description, and icon.',
        status: 'Pass',
        evidence: 'frontend WeatherForecastSection success test passed.',
      },
      {
        id: 'FT-WEATHER-002',
        module: 'Date Filtering',
        scenario: 'Forecast cards are filtered by selected date range.',
        steps: 'Select a date range and request forecast.',
        expected: 'Only forecast days inside the selected range remain visible.',
        actual: 'Component test displayed only the forecast day inside the selected range.',
        status: 'Pass',
        evidence: 'frontend WeatherForecastSection date-range test passed.',
      },
      {
        id: 'FT-WEATHER-003',
        module: 'Validation And Error',
        scenario: 'Blank or invalid city shows validation/error feedback.',
        steps: 'Submit blank city and simulate a failed forecast response.',
        expected: 'Blank city is blocked before API call; API failure displays an error.',
        actual: 'Component tests rendered validation and API error messages.',
        status: 'Pass',
        evidence: 'frontend WeatherForecastSection validation/error tests passed.',
      },
      {
        id: 'FT-WEATHER-004',
        module: 'Live Provider',
        scenario: 'Live Open-Meteo forecast is visible for a real destination.',
        steps: 'Submit a real city against the local backend weather route.',
        expected: 'Live provider response renders forecast values.',
        actual: 'Live provider path was not captured in browser during this run.',
        status: 'Blocked',
        evidence: 'No live browser forecast screenshot/result captured.',
      },
    ],
  },
  {
    name: 'Eco Route Planner',
    objective: 'Verify origin/destination route alternatives, CO2 estimates, geolocation allowed/denied, invalid input, and loading states.',
    environment: 'Implemented route planner component/backend route; no dedicated automated functional test exists.',
    resultSummary: 'Route planner implementation exists and API key is configured, but visible route-planning behavior was not executed in this run.',
    testCases: [
      {
        id: 'FT-ROUTE-001',
        module: 'Route Alternatives',
        scenario: 'Origin/destination route shows walking, cycling, and driving options.',
        steps: 'Enter origin and destination in the route planner.',
        expected: 'Route alternatives show distance, duration, and CO2 estimate/savings.',
        actual: 'Not executed as visible UI evidence.',
        status: 'Blocked',
        evidence: 'No direct route planner browser/API functional test available in current suite.',
      },
      {
        id: 'FT-ROUTE-002',
        module: 'Geolocation',
        scenario: 'Current-location flow handles allowed and denied browser geolocation.',
        steps: 'Use current location, allow permission, then deny permission.',
        expected: 'Allowed permission fills current origin; denial shows useful fallback/error.',
        actual: 'Not executed because interactive browser permission control was unavailable.',
        status: 'Blocked',
        evidence: 'No Playwright/Cypress/Selenium/Puppeteer runner installed.',
      },
      {
        id: 'FT-ROUTE-003',
        module: 'Invalid Route Input',
        scenario: 'Invalid origin/destination is rejected or shown as unavailable.',
        steps: 'Submit unknown or incomplete route input.',
        expected: 'Validation/error state is visible.',
        actual: 'Not executed as visible UI evidence.',
        status: 'Blocked',
        evidence: 'Route planner component and backend route exist; no captured functional run.',
      },
    ],
  },
  {
    name: 'Carbon Footprint Calculator, Insights, Offsets, And History',
    objective: 'Verify emissions calculation, invalid input, insights, offsets, authenticated history, summary, delete, and empty states.',
    environment: 'Frontend component/unit tests, backend utility tests, and analytics integration evidence.',
    resultSummary: 'Calculation math, insights, offset UI, and downstream analytics summary evidence passed. Full authenticated history save/delete browser flow was not completed.',
    testCases: [
      {
        id: 'FT-CARBON-001',
        module: 'Emission Calculation',
        scenario: 'Valid travel inputs calculate total emissions and category breakdown.',
        steps: 'Enter flight, car, lodging, rail, bus, or taxi inputs and calculate.',
        expected: 'Total and per-category emissions are displayed.',
        actual: 'Frontend and backend calculator tests passed representative travel inputs and aggregate totals.',
        status: 'Pass',
        evidence: 'frontend calculate tests passed; backend carbonCalculator tests passed.',
      },
      {
        id: 'FT-CARBON-002',
        module: 'Insights And Offsets',
        scenario: 'Impact insights identify largest source and offsets show suggestions/links.',
        steps: 'Calculate a trip and inspect insight/offset sections.',
        expected: 'Largest source, tree equivalent, breakdown, and external offset links appear.',
        actual: 'ImpactInsights and CarbonOffset tests rendered tree equivalents, percentages, largest source guidance, and credit organization links.',
        status: 'Pass',
        evidence: 'frontend ImpactInsights and CarbonOffset tests passed.',
      },
      {
        id: 'FT-CARBON-003',
        module: 'History And Summary',
        scenario: 'Authenticated calculations save to history, update summary, and delete cleanly.',
        steps: 'Calculate as authenticated user, open history/summary, delete an entry.',
        expected: 'History contains saved record, summary updates, and deletion removes record.',
        actual: 'Downstream analytics integration verified saved carbon entries aggregate correctly; visible history save/delete flow was not completed.',
        status: 'Blocked',
        evidence: 'backend analytics integration passed; browser carbon history flow not captured.',
      },
      {
        id: 'FT-CARBON-004',
        module: 'Invalid Input And Empty State',
        scenario: 'Invalid numbers/missing fields are rejected and empty result state is clear.',
        steps: 'Submit negative or missing inputs and inspect no-result UI.',
        expected: 'Invalid input is rejected; empty insights state is clear.',
        actual: 'ImpactInsights rendered no-data state. Full calculator invalid-form browser evidence was not captured.',
        status: 'Blocked',
        evidence: 'frontend ImpactInsights empty-state test passed; invalid calculator UI flow not captured.',
      },
    ],
  },
  {
    name: 'Community Reviews, Challenges, Badges, And Leaderboard',
    objective: 'Verify public community data, review creation/validation/editing, helpful votes, challenge progress, badges, and leaderboard.',
    environment: 'Seeded community tables and frontend hook tests.',
    resultSummary: 'Public data loading, create review mutation, helpful votes, challenge join/progress, badges, and leaderboard fetches passed at hook/API-boundary level.',
    testCases: [
      {
        id: 'FT-COMMUNITY-001',
        module: 'Public Community',
        scenario: 'Public reviews, challenges, badges, and leaderboard load.',
        steps: 'Open community page and inspect public data sections.',
        expected: 'Reviews, challenges, badges, and leaderboard are available to view.',
        actual: 'Community tables were seeded and useCommunity loaded public data in tests.',
        status: 'Pass',
        evidence: `${evidenceSummary}; frontend useCommunity public-data test passed.`,
      },
      {
        id: 'FT-COMMUNITY-002',
        module: 'Reviews',
        scenario: 'Authenticated user creates a valid review and invalid review is rejected.',
        steps: 'Submit a review with rating/text, then submit invalid data.',
        expected: 'Valid review is created; invalid input is rejected.',
        actual: 'Review creation mutation and cache invalidation passed. Invalid review UI validation was not separately captured.',
        status: 'Blocked',
        evidence: 'frontend useCommunity create-review test passed; invalid visible form evidence not captured.',
      },
      {
        id: 'FT-COMMUNITY-003',
        module: 'Helpful Votes',
        scenario: 'Helpful vote toggles visible state/count.',
        steps: 'Click helpful on a review, then click again to remove.',
        expected: 'Helpful state/count updates without duplicate votes.',
        actual: 'useCommunity tests verified helpful vote create/delete mutation behavior.',
        status: 'Pass',
        evidence: 'frontend useCommunity helpful-vote test passed.',
      },
      {
        id: 'FT-COMMUNITY-004',
        module: 'Challenges And Badges',
        scenario: 'User joins challenge and progress updates points/badges/leaderboard.',
        steps: 'Join a challenge and update progress.',
        expected: 'Challenge progress changes and badges/leaderboard data refresh.',
        actual: 'useCommunity tests verified challenge join/progress mutation behavior and related data invalidation.',
        status: 'Pass',
        evidence: 'frontend useCommunity challenge-progress test passed.',
      },
    ],
  },
  {
    name: 'Analytics, Reports, And Goals',
    objective: 'Verify dashboard metrics, empty states, PDF reports, annual carbon budget goals, invalid goals, and threshold statuses.',
    environment: 'Backend analytics API/database integration tests and frontend dashboard analytics tests.',
    resultSummary: 'Authenticated impact summaries, empty state, report actions, goal save/update, invalid goal handling, and alert thresholds passed through automated evidence.',
    testCases: [
      {
        id: 'FT-ANALYTICS-001',
        module: 'Dashboard Data',
        scenario: 'Analytics dashboard loads carbon saved, totals, charts, and detail rows.',
        steps: 'Open dashboard analytics with saved carbon records.',
        expected: 'Metrics, breakdowns, over-time data, and detail rows match saved records.',
        actual: 'Backend analytics integration aggregated per-user carbon entries; frontend dashboard analytics tests rendered metrics and detail data.',
        status: 'Pass',
        evidence: 'backend analytics integration tests passed; frontend dashboard analytics tests passed.',
      },
      {
        id: 'FT-ANALYTICS-002',
        module: 'Empty State',
        scenario: 'Empty analytics state appears when user has no carbon records.',
        steps: 'Open analytics dashboard as a user with no records.',
        expected: 'Dashboard reports no data and disables unavailable actions where appropriate.',
        actual: 'Backend and frontend tests covered empty impact data and empty-period report states.',
        status: 'Pass',
        evidence: 'backend analytics integration tests passed; frontend dashboard analytics tests passed.',
      },
      {
        id: 'FT-ANALYTICS-003',
        module: 'PDF Reports',
        scenario: 'Monthly and annual PDF report buttons work when data exists.',
        steps: 'Click monthly and annual report buttons with available records.',
        expected: 'PDF generation uses dashboard data and creates report files.',
        actual: 'Frontend tests exercised PDF generation with jsPDF/autotable mocks and disabled empty-period buttons.',
        status: 'Pass',
        evidence: 'frontend dashboard analytics page tests passed.',
      },
      {
        id: 'FT-ANALYTICS-004',
        module: 'Goals',
        scenario: 'Annual carbon budget goal create/update, invalid goal, and threshold statuses work.',
        steps: 'Create/update goal, submit invalid value, and inspect progress thresholds.',
        expected: 'Valid goals persist; invalid values reject; status changes on-track/monitor/alert based on usage.',
        actual: 'Backend integration covered goal create/read/update and invalid goal validation; frontend tests covered save and alert state.',
        status: 'Pass',
        evidence: 'backend analytics integration tests passed; frontend dashboard analytics tests passed.',
      },
    ],
  },
  {
    name: 'Admin Dashboard, Users, Destinations, And Analytics',
    objective: 'Verify admin access, non-admin denial, users table, destinations table, analytics, and visible empty/loading/error states.',
    environment: 'Admin implementation and middleware present; no dedicated admin functional automation exists.',
    resultSummary: 'Admin pages and role-aware middleware are implemented, but visible admin workflows were not executed in this run.',
    testCases: [
      {
        id: 'FT-ADMIN-001',
        module: 'Admin Access',
        scenario: 'Admin user can access admin layout and non-admin is denied.',
        steps: 'Log in as admin and non-admin, then open /admin routes.',
        expected: 'Admin sees admin layout; non-admin redirects or receives denial.',
        actual: 'Route protection implementation exists; browser access with admin/non-admin sessions was not completed.',
        status: 'Blocked',
        evidence: 'frontend middleware and admin pages exist; no captured browser session evidence.',
      },
      {
        id: 'FT-ADMIN-002',
        module: 'Users Table',
        scenario: 'Admin users page displays user records.',
        steps: 'Open /admin/users as admin.',
        expected: 'User records are visible with useful table/loading/error states.',
        actual: 'Not executed as visible browser evidence.',
        status: 'Blocked',
        evidence: 'frontend/src/app/admin/users/page.tsx exists; no automated functional test available.',
      },
      {
        id: 'FT-ADMIN-003',
        module: 'Destinations Table',
        scenario: 'Admin destinations page displays destination/location records.',
        steps: 'Open /admin/destinations as admin.',
        expected: 'Destination records are visible and empty/loading/error states are handled.',
        actual: 'Not executed as visible browser evidence.',
        status: 'Blocked',
        evidence: 'functional-test location rows seeded; no admin browser evidence captured.',
      },
      {
        id: 'FT-ADMIN-004',
        module: 'Admin Analytics',
        scenario: 'Admin analytics page displays system metrics.',
        steps: 'Open /admin/analytics as admin.',
        expected: 'Admin analytics metrics render successfully.',
        actual: 'Not executed as visible browser evidence.',
        status: 'Blocked',
        evidence: 'frontend/src/app/admin/analytics/page.tsx exists; no dedicated admin functional test available.',
      },
    ],
  },
]

function paragraph(text, options = {}) {
  return new Paragraph({
    ...options,
    spacing: options.spacing || { after: 120 },
    children: [new TextRun(String(text))],
  })
}

function heading(text, level = HeadingLevel.HEADING_1) {
  return paragraph(text, {
    heading: level,
    spacing: { before: 240, after: 120 },
  })
}

function cell(text, bold = false) {
  return new TableCell({
    margins: { top: 80, bottom: 80, left: 80, right: 80 },
    children: [
      new Paragraph({
        children: [new TextRun({ text: String(text), bold })],
      }),
    ],
  })
}

function testCaseTable(testCases) {
  const headers = [
    'Test ID',
    'Module/Feature',
    'Scenario',
    'Input/Steps',
    'Expected Result',
    'Actual Result',
    'Status',
    'Evidence',
  ]

  return new Table({
    width: { size: 100, type: WidthType.PERCENTAGE },
    rows: [
      new TableRow({
        tableHeader: true,
        children: headers.map((header) => cell(header, true)),
      }),
      ...testCases.map((testCase) =>
        new TableRow({
          children: [
            testCase.id,
            testCase.module,
            testCase.scenario,
            testCase.steps,
            testCase.expected,
            testCase.actual,
            testCase.status,
            testCase.evidence,
          ].map((value) => cell(value)),
        }),
      ),
    ],
  })
}

const passCount = modules.reduce(
  (total, module) => total + module.testCases.filter((testCase) => testCase.status === 'Pass').length,
  0,
)
const blockedCount = modules.reduce(
  (total, module) => total + module.testCases.filter((testCase) => testCase.status === 'Blocked').length,
  0,
)
const failCount = modules.reduce(
  (total, module) => total + module.testCases.filter((testCase) => testCase.status === 'Fail').length,
  0,
)
const totalCount = passCount + blockedCount + failCount

const children = [
  new Paragraph({
    heading: HeadingLevel.TITLE,
    alignment: AlignmentType.CENTER,
    children: [new TextRun('Terratrace Project Functional Test Report')],
  }),
  paragraph(`Report date: ${reportDate}`),
  paragraph('Reference: docs/REQUIREMENTS.md, docs/feature-implementation-status.md, test/functional-testing-agent-guide.md, and test/test-list.md.'),
  paragraph('Report scope: whole Terratrace project. Each module is documented as its own functional-test section.'),
  heading('Objective'),
  paragraph('Verify that visible Terratrace system features behave according to requirements, including success, failure, and edge-case behavior.'),
  heading('Environment And Tools'),
  paragraph('Primary local environment: PostgreSQL terratrace-postgres, local backend, local Next frontend, Firefox headless attempt, Vitest, Testing Library, and backend API/database integration suites.'),
  paragraph(`Evidence summary file: ${evidenceSummary}.`),
  paragraph('Fresh command evidence: frontend Vitest passed 20 files and 93 tests; targeted Green Itinerary Vitest passed 2 files and 13 tests; backend Vitest passed 7 files and 39 tests after removing a disposable seed-data collision.'),
  paragraph('Local route evidence: GET http://localhost:3003/api/locations returned functional-test seed rows; GET http://localhost:3002/eco-directory returned HTTP 200 rendered HTML.'),
  paragraph('Screenshot limitation: headless Firefox capture was attempted but blocked by existing Firefox profile/process behavior and a headless render hang, so this report uses route/API/terminal/component evidence instead of screenshot files.'),
  heading('Coverage Summary'),
  paragraph(`Module sections: ${modules.length}. Functional rows documented: ${totalCount}. Pass: ${passCount}. Fail: ${failCount}. Blocked: ${blockedCount}.`),
  paragraph('Pass is used only where observed UI/component behavior, local route output, or API-backed user-visible behavior matched the expected outcome. Blocked is used for flows requiring unavailable interactive browser automation, external provider confirmation, or admin authenticated sessions not captured in this run.'),
]

for (const module of modules) {
  children.push(
    heading(`Module Section: ${module.name}`, HeadingLevel.HEADING_2),
    paragraph(`Objective: ${module.objective}`),
    paragraph(`Environment: ${module.environment}`),
    paragraph(`Result summary: ${module.resultSummary}`),
    testCaseTable(module.testCases),
  )
}

children.push(
  heading('Conclusion'),
  paragraph('Terratrace has passing functional evidence for the strongest automated and locally observed surfaces: authentication/profile, eco-directory, favourites, trips and green itinerary, recommendation/weather components, carbon calculation/insights, community hooks, and analytics/reporting/goals. Remaining blocked rows are mainly external-service confirmations, route geolocation permission states, carbon history delete, and admin dashboards.'),
)

const doc = new Document({
  sections: [
    {
      properties: {},
      children,
    },
  ],
})

await writeFile(outputPath, await Packer.toBuffer(doc))
console.log(`Wrote ${outputPath}`)
