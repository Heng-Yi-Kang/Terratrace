/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable experimental features if needed
  experimental: {
    // serverActions are stable in Next.js 14, no longer experimental
  },
}

module.exports = nextConfig